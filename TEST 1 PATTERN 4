#include <iostream>
#include <cmath>

int main() {
    int rows = 5;
    int num = 2;

    for (int i = 1; i <= rows; ++i) {
        for (int j = 1; j <= i; ++j) {
            std::cout << num << " ";
        }
        num = pow(num, 2);
        std::cout << std::endl;
    }

    return 0;
}
